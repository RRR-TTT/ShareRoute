<%= form_for(@point_picture) do |f| %>
  <% if @point_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@point_picture.errors.count, "error") %> prohibited this point_picture from being saved:</h2>

      <ul>
      <% @point_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :PointPictureImage %><br>
    <%= f.text_field :PointPictureImage %>
  </div>
  <div class="field">
    <%= f.label :PostingUser_id %><br>
    <%= f.text_field :PostingUser_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
